{
  "name": "pixel-mech-battle",
  "version": "1.0.0",
  "description": "A retro pixel-style mech battle game with MVVM architecture",
  "main": "dist/app.js",
  "scripts": {
    "build": "tsc && node bundle.js",
    "dev": "npx esbuild src/game.js --bundle --outfile=dist/game.js && npx http-server . -p 3000 -o"
  },
  "keywords": ["game", "pixel", "mech", "battle", "typescript"],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "typescript": "^5.0.0",
    "esbuild": "^0.17.0"
  }
}
